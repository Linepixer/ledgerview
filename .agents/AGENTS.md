
## Git Restrictions
- **NEVER** execute git commands to commit (git commit) or push (git push).
- Committing and pushing are exclusively the user's responsibility.
- You may only **suggest** when it is a good time to commit and **propose** commit messages.
